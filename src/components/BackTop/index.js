import React, { PureComponent } from "react";
import './index.scss'

class BackTop extends PureComponent {
  render() {
    return (
      <span
        className="m-back"
      >
        回到顶部
      </span>
    );
  }
}

export default BackTop;
